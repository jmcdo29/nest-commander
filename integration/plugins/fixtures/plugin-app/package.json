{
  "name": "test-plugin",
  "version": "0.0.0",
  "description": "This is only a simulated test package for plugins",
  "main": "./dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rimraf dist && tsc"
  },
  "author": "",
  "license": "",
  "devDependencies": {
    "rimraf": "^3.0.2"
  },
  "dependencies": {
    "nest-commander": "workspace:^2.3.3"
  }
}