import { QuestionSet, Question } from 'nest-commander';

@QuestionSet({ name: '<%= name %>' })
export class <%= classify(name) %>Questions {
  @Question({
    name: 'default',
  })
  parseDefault(str: string): string {
    return str;
  }
}
